#include <iostream>
#include <string>
using namespace std;

int N;

string systemAddition(string m, string n) {
	string sum;
	for (int i = 0; i < m.size(); i++) {
		if (m[i]+n[i]-96 > N){

		}
	}
}

int main() {
	string M;
	int i = 1;
	cin >> N >> M;

	/*while (true) {
		int sum = systemAddition(M, _M);
		if (i > 30) {
			cout << "Impossible!";
			break;
		}
		else if () {
			cout <<"STEP="<<i;
			break;
		}
		else {
			i++;
			M = sum;
			_M = reverseNumber(M);
		}
	}*/

	return 0;
}